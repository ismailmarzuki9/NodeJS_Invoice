CREATE TABLE invoice (
    id_in VARCHAR PRIMARY KEY,
    no INTEGER,
    description TEXT,
    tic_number VARCHAR(50),
    nominal NUMERIC(15,2),
    total NUMERIC(15,2),
    date_issuance VARCHAR(50),
    Due_Date VARCHAR(50),
    nama_instansi VARCHAR(25),
    alamat VARCHAR(50),
    teruntuk VARCHAR(20),
    tggl_data_masuk TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    tggl_data_terupdate TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE users (
    id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,

    email VARCHAR(255) NOT NULL UNIQUE,
    username VARCHAR(100) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,

    is_active BOOLEAN NOT NULL DEFAULT FALSE,
    email_verified BOOLEAN NOT NULL DEFAULT FALSE,

    failed_login_attempts INTEGER NOT NULL DEFAULT 0,
    locked_until TIMESTAMPTZ NULL,

    last_login_at TIMESTAMPTZ NULL,

    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

npm install express mysql2 sequelize jsonwebtoken dotenv cookie-parser cors
// try{
//     for(let i=0; i < tic.number.length ; i++ )
//     {
//         const id = req.body.id_in[i]
//         const description = req.body.description[i]
//         const nominal = req.body.nominal[i]
//         const id = req.body.id_in[i]
//     }
// }catch{

// }

==========
static async editinvoicepost(req, res){
        console.log("Data from edit", req.body);
        try{
             const {
                        id_in,
                        no,
                        description,
                        tic_number,
                        nominal,
                        total,
                        date_issuance,
                        due_date
                    } = req.body; //Perlu di descripsikan

            for (let i = 0; i < description.length; i++) {
                console.log("data Ke",id_in[i]);
                if (description[i].length=id_in[i].length) {
                    console.log("Id_kosong");
                }
                await invoicemodel.update(id_in[i], {
                    id_in : id_in[i],
                    no : no,
                    description : description[i],
                    tic_number : tic_number[i],
                    nominal : nominal[i],
                    total,
                    date_issuance,
                    due_date
                })
            }

            res.redirect('/print/'+no);

        }catch (error){
            console.error(error);
            // kirim respon gagal
            // return res.status(500).json("Gagal menyimpan data");
        }
    }
==========


==S=============
ada issue di edit diaman ada data hapus yang tidak di proses dan data edit yang tidak tesimpan [sudah solve]
==E=============